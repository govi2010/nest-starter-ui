import {getUrl} from './base';

export const AccountUrl = {
  login: getUrl('account/login'),
  register: getUrl('account/register'),
  info: getUrl('account/info'),
  update: getUrl('account/update')
};
